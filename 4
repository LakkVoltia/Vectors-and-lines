#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

using namespace std;
int main(){
    vector<string> lines;
    string line;
    while(getline(cin,line)){
    lines.push_back(line);
    }
    
    sort(lines.rbegin(), lines.rend());
    
    for (size_t i=0; i != lines.size(); i++){
        cout <<lines[i] << "\n";
    }
}
